

# Import general options
import /etc/caddy/Caddyfile.general

# Import common snippets
import /etc/caddy/Caddyfile.snippets

# Import common site
import /etc/caddy/Caddyfile.site

# Fallback to angular_frontend
handle {
	# By default redirect to Angular frontend
	import angular_frontend
}
