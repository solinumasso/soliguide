
load(`${__dirname}/utils.mongodb`);
load(`${__dirname}/entity_update.mongodb`);

const anonymizePlaces = async () => {
  await entityUpdate(
    db.lieux,
    "not public places",
    {
      status: { $ne: "ONLINE" },
    },
    "",
    "$lieu_id"
  );
};
