
load(`${__dirname}/utils.mongodb`);

const anonymizeInvitations = async () =>
  await updateMany(
    db.invitations,
    "invitations user data",
    stringFieldFilter("createdUser.mail"),
    [
      {
        $set: emailAggregate("createdUser.mail", "$user_id"),
      },
    ]
  );
