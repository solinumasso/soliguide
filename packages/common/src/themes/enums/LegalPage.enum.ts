/**
 * The legal documents published by every Soliguide/Soliguia website.
 *
 * The values double as the canonical page keys used by the Angular frontend
 * routing table, so they must stay stable.
 */
export enum LegalPage {
  LEGAL_NOTICES = "legal-notices",
  PRIVACY_POLICY = "privacy-policy",
  DATA_PROCESSING_AGREEMENT = "data-processing-agreement",
  COOKIE_POLICY = "cookie-policy",
  GCU = "gcu",
}
