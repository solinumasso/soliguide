/**
 * @deprecated Use `CampaignSectionPath` + `CAMPAIGN_SECTION_PATHS`.
 * Ancien vocabulaire des sections modifiables via les crisis campaigns
 * (4 valeurs seulement).
 */
export enum CampaignChangesSection {
  services = "services",
  tempClosure = "tempClosure",
  tempHours = "tempHours",
  tempMessage = "tempMessage",
}
